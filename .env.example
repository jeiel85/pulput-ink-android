# OPENAI_API_KEY: required.
# Used for both speech-to-text (Whisper) and AI post-processing
# (transcript correction, summary, sermon outline, scripture extraction).
# Get a key at https://platform.openai.com/api-keys — it must start with "sk-".
OPENAI_API_KEY=MY_OPENAI_API_KEY
